package cy.agorise.bitsybitshareswallet.utils


object Constants {

    /**
     * Key used to store the night mode setting into the shared preferences
     */
    val KEY_NIGHT_MODE_ACTIVATED = "key_night_mode_activated"

    /**
     * Key used to store the close app after minutes into the shared preferences
     */
    val KEY_MINUTES_CLOSE_MODE_ACTIVATED = "key_minutes_activated"

    /**
     * Key used to store if licence is agreed already or not
     */
    val KEY_LICENCE_AGREED = "key_licence_agreed"
}
